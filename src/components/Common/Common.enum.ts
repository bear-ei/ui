export enum LayoutType {
        HORIZONTAL = 'HORIZONTAL',
        VERTICAL = 'VERTICAL'
}

export enum Density {
        COMFORTABLE = 'COMFORTABLE',
        COMPACT = 'COMPACT',
        STANDARD = 'STANDARD'
}

export enum DensityScale {
        COMFORTABLE = -1,
        COMPACT = -2,
        STANDARD = 0
}
