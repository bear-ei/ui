export enum NavigationRailAnimatedType {
        STANDARD = 'STANDARD',
        COLLAPSE = 'COLLAPSE'
}

export enum NavigationRailType {
        SEGMENT = 'SEGMENT',
        BLOCK = 'BLOCK'
}

export enum DestinationPosition {
        TOP = 'TOP',
        CENTER = 'CENTER',
        BOTTOM = 'BOTTOM'
}
