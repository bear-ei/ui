import {View} from 'react-native'
import styled from 'styled-components/native'
import type {ContainerProps} from './Menu.interface'

export const Container = styled(View)<ContainerProps>`
	flex: 1;
`
